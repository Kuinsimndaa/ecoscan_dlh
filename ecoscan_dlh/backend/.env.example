# ========================================
# Backend Environment Configuration
# ========================================
# This file is for LOCAL DEVELOPMENT (npm start)
# For Docker deployment, use root .env file instead

# ===== Server Configuration =====
NODE_ENV=development
PORT=3030

# ===== Database Configuration =====
# For local npm start (non-Docker):
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=dlh_ecoscan
DB_CONNECTION_LIMIT=10

# For Docker Compose deployment:
# DB_HOST=mysql (not localhost!)
# DB_USER=ecoscan_user
# DB_PASSWORD=from root .env file

# ===== CORS Configuration =====
# Frontend URL that can access this API
# Local development: http://localhost:5050 (Vite dev server)
# Docker: http://localhost (Nginx)
CORS_ORIGIN=http://localhost:5050

# ========================================
# SETUP INSTRUCTIONS
# ========================================

# 1. COPY THIS FILE
#    Copy-Item .env.example .env

# 2. UPDATE DATABASE PASSWORD
#    Set DB_PASSWORD to match your local MySQL/MariaDB

# 3. CHOOSE RUN METHOD
#
#    Method A: Local npm (for development)
#      - Use DB_HOST=localhost
#      - Use DB_USER=root
#      - Use CORS_ORIGIN=http://localhost:5050
#      - Run: npm start
#
#    Method B: Docker Compose (for testing deployment)
#      - Use root .env file instead
#      - Run: docker-compose up -d

# ========================================
# NOTES
# ========================================
# - This .env is for backend local development only
# - For Docker, backend reads from root .env via docker-compose.yml
# - CORS_ORIGIN must match frontend URL exactly
# - Port 3030 is the backend API port (don't change unless needed)
